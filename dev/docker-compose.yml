# Development Environment for BambooClaw Core Agentic Testing
#
# Use this for:
# - Running the agent in a sandboxed environment
# - Testing dangerous commands safely
# - Developing new skills/integrations
#
# Usage:
#   cd dev && ./cli.sh up
#   or from root: ./dev/cli.sh up
name: BambooClaw Core-dev
services:
  # ── The Agent (Development Image) ──
  # Builds from source using the 'dev' stage of the root Dockerfile
  BambooClaw Core-dev:
    build:
      context: ..
      dockerfile: Dockerfile
      target: dev
    container_name: BambooClaw Core-dev
    restart: unless-stopped
    environment:
      - API_KEY
      - PROVIDER
      - BambooClaw Core_MODEL
      - BambooClaw Core_GATEWAY_PORT=3000
      - SANDBOX_HOST=BambooClaw Core-sandbox
    volumes:
      # Mount single config file (avoids shadowing other files in .BambooClaw Core)
      - ../target/.BambooClaw Core/config.toml:/BambooClaw Core-data/.BambooClaw Core/config.toml
      # Mount shared workspace
      - ../playground:/BambooClaw Core-data/workspace
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - dev-net

  # ── The Sandbox (Ubuntu Environment) ──
  # A fully loaded Ubuntu environment for the agent to play in.
  sandbox:
    build:
      context: sandbox # Context relative to dev/
      dockerfile: Dockerfile
    container_name: BambooClaw Core-sandbox
    hostname: dev-box
    command: ["tail", "-f", "/dev/null"]
    working_dir: /home/developer/workspace
    user: developer
    environment:
      - TERM=xterm-256color
      - SHELL=/bin/bash
    volumes:
      - ../playground:/home/developer/workspace # Mount local playground
    networks:
      - dev-net

networks:
  dev-net:
    driver: bridge
